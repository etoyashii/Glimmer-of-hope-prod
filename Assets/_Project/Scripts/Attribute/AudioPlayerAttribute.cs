using UnityEngine;

namespace GlimmerOfHope.Editor
{
    /// <summary>
    /// Affiche un player audio avec Waveform dans l'éditeur.
    /// Contrôles : Play/Stop, Trim, Progression. Autres paramètres via AudioSource.
    /// Usage : [AudioPlayer] public AudioClip clip;
    /// </summary>
    public class AudioPlayerAttribute : PropertyAttribute
    {
    
    }
}
